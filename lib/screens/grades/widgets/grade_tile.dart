import 'package:flutter/material.dart';

class GradeTile extends StatelessWidget {
  const GradeTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
